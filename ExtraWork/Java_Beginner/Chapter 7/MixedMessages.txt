| Code Candidate               | Output                       | Unused Output               |
| ---------------------------- | ---------------------------- | --------------------------- |
| `b.m1(); c.m2(); a.m3();`    | `B's m1, A's m2, A's m3, 3`  | A's m1, A's m2, C's m3, 6   |
| `c.m1(); c.m2(); c.m3();`    | `B's m1, A's m2, C's m3, 13` | B's m1, C's m2, A's m3      | 
| `a.m1(); b.m2(); c.m3();`    | `A's m1, B's m2, A's m3, 3`  | A's m1, A's m2, C's m3, 13  |
| `a2.m1(); a2.m2(); a2.m3();` | `B's m1, A's m2, C's m3, 6`  |                             |

class A {
    int m1() { return 1; }
    int m2() { return 2; }
    int m3() { return 3; }
}

class B extends A {
    int m1() { return 10; }  // overrides A's m1
}

class C extends B {
    int m2() { return 20; }  // overrides B's m2 (which inherited A's)
}

